# Cart Service (Deprecated & Merged)

This module has been deprecated and merged directly into `orderservice`.
All cart operations (`/api/cart/**`) are now served by `orderservice` with PostgreSQL persistence.
