spring:
  application:
    name: authservice

server:
  port: 8086 # Default to 8086

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

keycloak:
  server-url: http://localhost:9090
  realm: order
  client-id: order
  client-secret: CJPgXLLB4ha8bIVTvRyQflm1LZKf1NcY

